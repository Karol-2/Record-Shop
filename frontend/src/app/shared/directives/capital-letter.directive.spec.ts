import { CapitalLetterDirective } from './capital-letter.directive';

describe('CapitalLetterDirective', () => {
  it('should create an instance', () => {
    const directive = new CapitalLetterDirective();
    expect(directive).toBeTruthy();
  });
});
