import { AuctionTitlePipe } from './auction-title.pipe';

describe('AuctionTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new AuctionTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
