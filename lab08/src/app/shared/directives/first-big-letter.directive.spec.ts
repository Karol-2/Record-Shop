import { FirstBigLetterDirective } from './first-big-letter.directive';

describe('FirstBigLetterDirective', () => {
  it('should create an instance', () => {
    const directive = new FirstBigLetterDirective();
    expect(directive).toBeTruthy();
  });
});
