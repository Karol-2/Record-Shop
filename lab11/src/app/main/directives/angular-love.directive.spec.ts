import { AngularLoveDirective } from './angular-love.directive';

describe('AngularLoveDirective', () => {
  it('should create an instance', () => {
    const directive = new AngularLoveDirective();
    expect(directive).toBeTruthy();
  });
});
