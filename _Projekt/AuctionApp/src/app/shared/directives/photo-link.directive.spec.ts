import { PhotoLinkDirective } from './photo-link.directive';

describe('PhotoLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new PhotoLinkDirective();
    expect(directive).toBeTruthy();
  });
});
