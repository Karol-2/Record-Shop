import { ProduktInfoPipe } from './produkt-info.pipe';

describe('ProduktInfoPipe', () => {
  it('create an instance', () => {
    const pipe = new ProduktInfoPipe();
    expect(pipe).toBeTruthy();
  });
});
